import styled from 'styled-components';
  
export const Button = styled.div`
    position: fixed; /*Position is fixed regardless of scroll*/
    left: 90%; /*Position is fixed at 90% from the left*/
    bottom: 5px; /*Position is 5px from the bottom*/
    font-size: 3rem; /*Size of button is at 3rem, rem is a unit relative to the root element*/
    color: orange; /*Colour is orange*/
`